#!/bin/bash
# Get the ngrok tunnel URL from the file written by the ngrok container
# Usage: bin/get-tunnel-url

TUNNEL_FILE="tmp/tunnel_url"

if [ -f "$TUNNEL_FILE" ]; then
  cat "$TUNNEL_FILE"
else
  echo "(not available - set NGROK_AUTHTOKEN in .env.development)"
fi
