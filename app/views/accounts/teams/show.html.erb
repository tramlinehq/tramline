<%= render V2::TabbedContainerComponent.new(title: current_organization.name,
                                            turbo_frame: dom_id(current_organization, :edit_org),
                                            tab_config: @tab_configuration) do |container| %>
  <% container.with_back_button %>
  <% container.with_tab do %>
    <%= render partial: "accounts/teams/show" %>
  <% end %>
<% end %>
