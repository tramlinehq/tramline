{
  "blocks": [
    {
      "type": "section",
      "text": {
        "type": "mrkdwn",
        "text": ":x: Failed to trigger *<%= @workflow_kind %>* workflow run to build *<%= @release_version %> (<%= @build_number %>)*."
      }
    }
  ]
}
