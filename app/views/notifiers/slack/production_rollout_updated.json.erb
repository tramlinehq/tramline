{
  "blocks": [
    {
      "type": "section",
      "text": {
        "type": "mrkdwn",
        "text": "The rollout for release *<%= @release_version %> (<%= @build_number %>)* is *active*, and it is currently on stage *<%= @current_stage %> (<%= @rollout_percentage %>%)*."
      }
    }
  ]
}
