{
  "blocks": [
    {
      "type": "section",
      "text": {
        "type": "mrkdwn",
        "text": ":sparkles: The internal build step finished for *<%= @release_version %> (<%= @build_number %>)* for commit <<%= @commit_url %>|<%= @commit_sha %>>. The build has been sent to <%= @submission_channels %>"
      }
    },
    {
      "type": "context",
      "elements": [
        {
          "type": "mrkdwn",
          "text": "- Changes since the last release are sent within this message thread."
        }
      ]
    }
  ]
}
