{
  "blocks": [
    {
      "type": "section",
      "text": {
        "type": "mrkdwn",
        "text": "Deployment of *<%= @deployment_channel_type %>* build *<%= @release_version %> (<%= @build_number %>)* to *<%= deployment_channel_display_name %>* failed! :rotating_light:"
      }
    }
  ]
}
