<%= turbo_stream.replace @stream_id do %>
  <% content_tag(:div, id: @stream_id) do %>
    <%= render V2::LiveRelease::MetadataComponent.new(@release,
                                                      @active_languages,
                                                      @language,
                                                      android_metadata: @android_metadata,
                                                      ios_metadata: @ios_metadata) %>
  <% end %>
<% end %>
