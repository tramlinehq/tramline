<%= render V2::LiveRelease::ContainerComponent.new(@release, title: @train.name) do |container| %>
  <% container.with_back_button %>
  <% container.with_tab(title: "Notes — Release Metadata", icon: live_release_tab_configuration.dig(:metadata, :notes, :icon)) do %>
    <% content_tag(:div, id: @stream_id) do %>
      <%= render V2::LiveRelease::MetadataComponent.new(@release,
                                                        @active_languages,
                                                        @language,
                                                        ios_metadata: @ios_metadata,
                                                        android_metadata: @android_metadata) %>
    <% end %>
  <% end %>
<% end %>
