<%= render ButtonComponent.new(
      label: "Refresh workflows",
      scheme: :light,
      type: :link,
      size: :xxs,
      options: refresh_workflows_app_train_platform_submission_config_path(app, train, platform_name),
      html_options: {
        method: :post,
        class: "-mx-1 mt-1",
        data: {
          turbo_method: :post,
          turbo_confirm: "Refreshing workflows will clear any unsaved changes on this page. Are you sure?"
        }
      }
    ) %>
