<% breadcrumb :train, @train %>

<%= render partial: 'shared/errors', locals: { resource: @step } %>

<%= form_with(model: @step, url: app_train_steps_path(@app, @train)) do |form| %>
  <div class="mb-8">
    <h1 class="text-2xl md:text-3xl text-slate-800 font-bold">Create a new step 🪜</h1>
  </div>

  <div class="border-t my-8"></div>

  <%= render 'form', form: form %>

  <div data-controller="nested-form">
    <div class="sm:flex sm:justify-between sm:items-center mt-8 mb-2 pb-5">
      <p class="font-bold text-xl">
        Where should we deploy?
      </p>

      <div class="grid grid-flow-col sm:auto-cols-max justify-start sm:justify-end gap-2">
        <%= button_tag type: "button", data: { action: "nested-form#addAssociation" }, class: "btn bg-emerald-500 hover:bg-emerald-600 text-white" do %>
          <svg class="w-4 h-4 fill-current opacity-50 shrink-0" viewBox="0 0 16 16">
            <path d="M15 7H9V1c0-.6-.4-1-1-1S7 .4 7 1v6H1c-.6 0-1 .4-1 1s.4 1 1 1h6v6c0 .6.4 1 1 1s1-.4 1-1V9h6c.6 0 1-.4 1-1s-.4-1-1-1z"></path>
          </svg>

          <span class="ml-2">Add a new deployment</span>
        <% end %>
      </div>
    </div>

    <div class="pb-5">
      <%= render "shared/note_box",
                 message: "The generated build artifact above will be moved across the configured deployment channels below." %>
    </div>

    <div class="grid gap-5 md:grid-cols-1 p-2">
      <template data-nested-form-target='template'>
        <%= form.fields_for :deployments, Deployment.new, child_index: 'TEMPLATE_RECORD' do |deployment_form| %>
          <%= render partial: "deployment", locals: { form: deployment_form, train: @train } %>
        <% end %>
      </template>

      <%= form.fields_for :deployments, Deployment.new do |deployment_form| %>
        <%= render partial: "deployment", locals: { form: deployment_form, train: @train } %>
      <% end %>

      <div data-nested-form-target="addItem"></div>
    </div>
  </div>


  <div class="mt-5">
    <%= form.submit 'Create', class: "btn bg-indigo-500 hover:bg-indigo-600 text-white" do %>
      <svg class="w-4 h-4 fill-current opacity-50 shrink-0" viewBox="0 0 16 16">
        <path d="M15 7H9V1c0-.6-.4-1-1-1S7 .4 7 1v6H1c-.6 0-1 .4-1 1s.4 1 1 1h6v6c0 .6.4 1 1 1s1-.4 1-1V9h6c.6 0 1-.4 1-1s-.4-1-1-1z"/>
      </svg>
    <% end %>
  </div>
<% end %>
