<%= render V2::ContainerComponent.new(title: "Create a new train", error_resource: @train) do |container| %>
  <% container.with_back_button(to: "the app", path: app_path(@app)) %>
  <% container.with_body do %>
    <%= render 'form', app: @app, train: @train %>
  <% end %>
<% end %>
