<%= render Form::SwitchComponent.new(form:,
                                         field_name: :tag_platform_releases,
                                         on_label: "Platform Specific Tags enabled",
                                         off_label: "Platform Specific Tags disabled",
                                         hide_child: !@train.tag_platform_releases?) do |component| %>
  <% component.with_child do %>
    <%= render Form::SwitchComponent.new(form:,
                                             field_name: :tag_all_store_releases,
                                             on_label: "Tag All Store Releases enabled",
                                             off_label: "Tag All Store Releases disabled") do |sub_component| %>
      <% sub_component.with_heading do %>
        Tag All Store Releases
      <% end %>
      <% sub_component.with_description do %>
        Always tag a build that reaches the store production channel.
        <br/>There can be multiple such builds when there are release fixes during staged rollout.
        <br/>By default, only the final build of the release is tagged.
      <% end %>
    <% end %>
  <% end %>
<% end %>
