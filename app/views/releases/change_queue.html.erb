<%= render V2::LiveRelease::ContainerComponent.new(@release,
                                                   title: @train.name,
                                                   turbo_frame: dom_id(@release, :live_release),
                                                   tab_config: @tab_configuration) do |container| %>
  <% container.with_back_button %>
  <% container.with_tab(title: "Changeset tracking", icon: @tab_configuration.dig(:kickoff, :changeset_tracking, :icon)) do %>
    <%= render V2::LiveRelease::ChangeQueueComponent.new(@release) %>
  <% end %>
<% end %>
