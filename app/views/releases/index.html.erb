<% if current_user.pending_profile?(current_organization) %>
  <% content_for :sticky_top_message do %>
    <%= render AlertComponent.new(kind: :banner,
                                      type: :notice,
                                      title: "Incomplete user profile",
                                      dismissible: false,
                                      info: { label: "Complete profile", link: "/accounts/user/edit" }) do %>
      You haven't completed your profile yet. Add a GitHub Login ID and select the team you belong to.
    <% end %>
  <% end %>
<% end %>

<%= render ReleaseListComponent.new(train: @train) %>
