<%= render V2::LiveRelease::ContainerComponent.new(@release, title: @train.name) do |container| %>
  <% container.with_back_button %>
  <% container.with_tab(title: "Changeset tracking", icon: live_release_tab_configuration.dig(:kickoff, :changeset_tracking, :icon)) do %>
    <%= render V2::LiveRelease::ChangesetTrackingComponent.new(@release) %>
  <% end %>
<% end %>
