<%= render V2::LiveRelease::ContainerComponent.new(@release,
                                                   title: @train.name,
                                                   tab_config: @tab_configuration) do |container| %>
  <% container.with_back_button %>
  <% container.with_tab(title: "Release Candidate", icon: @tab_configuration.dig(:stability, :release_candidate, :icon)) do %>
    <%= render V2::LiveRelease::ReleaseCandidatesComponent.new(@release) %>
  <% end %>
<% end %>
