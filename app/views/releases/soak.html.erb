<%= render LiveRelease::ContainerComponent.new(@release, title: @train.name) do |container| %>
  <% container.with_back_button %>
  <% container.with_tab(title: "Soak Period", icon: live_release_tab_configuration.dig(:stability, :soak_period, :icon)) do %>
    <%= render LiveRelease::SoakComponent.new(@release, status: live_release_tab_configuration.dig(:stability, :soak_period, :status)) %>
  <% end %>
<% end %>
