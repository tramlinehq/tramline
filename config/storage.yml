test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

amazon:
  service: S3
  access_key_id: <%= Rails.application.credentials.dig(:dependencies, :aws, :access_key_id) %>
  secret_access_key: <%= Rails.application.credentials.dig(:dependencies, :aws, :secret_key_id) %>
  region: <%= Rails.application.credentials.dig(:dependencies, :aws, :region) %>
  bucket: <%= Rails.application.credentials.dig(:dependencies, :aws, :bucket) %>