{
  "additionalProperties": true,
  "description": "This webhook event is triggered when a release successfully completes.",
  "properties": {
    "platform": {
      "enum": [
        "android",
        "ios",
        "cross_platform"
      ],
      "title": "Platform",
      "type": "string"
    },
    "release_branch": {
      "title": "ReleaseBranch",
      "type": "string"
    },
    "release_version": {
      "title": "ReleaseVersion",
      "type": "string"
    }
  },
  "required": [
    "platform",
    "release_branch",
    "release_version"
  ],
  "title": "release.completed",
  "type": "object"
}
